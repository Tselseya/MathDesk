# Development endpoint for the existing local n8n workflow.
# Do not commit a real private endpoint or credentials.
VITE_N8N_WEBHOOK_URL=https://your-current-n8n-host/webhook/mathdesk-ai

# Optional future gateway endpoint. When set, the adapter can be switched
# without changing UI components.
# VITE_MATHDESK_API_URL=https://api.example.com
