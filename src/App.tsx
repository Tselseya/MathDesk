import { useEffect, useState } from 'react';
import { ArrowLeft, ArrowUpRight, BookOpen, GitBranch, PenLine, Sparkles, Upload } from 'lucide-react';
import AuthPanel from './components/AuthPanel';
import ChatWorkspace from './components/ChatWorkspace';
import ToolsDirectory from './components/ToolsDirectory';
import MotionToggle from './components/MotionToggle';
import type { MathDeskMode } from './types/ai';

const modes: Array<{ id: MathDeskMode; label: string; description: string }> = [{ id: 'solve', label: 'Solve', description: 'Work through a problem step by step.' }, { id: 'learn', label: 'Learn', description: 'Understand the idea behind the answer.' }, { id: 'practice', label: 'Practice', description: 'Generate exercises at your pace.' }];

export default function App() {
  const [workspaceOpen, setWorkspaceOpen] = useState(false); const [mode, setMode] = useState<MathDeskMode>('solve'); const [prompt, setPrompt] = useState('');
  useEffect(() => { const elements = document.querySelectorAll<HTMLElement>('.reveal-on-scroll'); if (!('IntersectionObserver' in window)) { elements.forEach((element) => element.classList.add('revealed')); return; } const observer = new IntersectionObserver((entries) => entries.forEach((entry) => { if (entry.isIntersecting) { entry.target.classList.add('revealed'); observer.unobserve(entry.target); } }), { threshold: .14 }); elements.forEach((element) => observer.observe(element)); return () => observer.disconnect(); }, []);
  function openWorkspace(nextMode: MathDeskMode, nextPrompt = '') { setMode(nextMode); setPrompt(nextPrompt); setWorkspaceOpen(true); }
  if (workspaceOpen) return <main className="app-shell"><nav className="topbar"><button className="back-link" onClick={() => setWorkspaceOpen(false)}><ArrowLeft size={17} /> Home</button><a className="brand" href="/" aria-label="MathDesk home"><span className="brand-mark">∑</span><span>Math<span>Desk</span></span></a><div className="topbar-actions"><span className="branch-pill">AI workspace</span><MotionToggle /><AuthPanel compact /></div></nav><ChatWorkspace initialMode={mode} initialPrompt={prompt} /></main>;
  return <main className="shell"><nav className="topbar"><a className="brand" href="/" aria-label="MathDesk home"><span className="brand-mark">∑</span><span>Math<span>Desk</span></span></a><div className="topbar-actions"><span className="branch-pill">learn / solve / practice</span><MotionToggle /><a className="icon-link" href="https://github.com/Tselseya/MathDesk" target="_blank" rel="noreferrer" aria-label="Open MathDesk on GitHub"><GitBranch size={18} /></a><AuthPanel compact /></div></nav><section className="hero"><div className="hero-copy"><p className="eyebrow"><Sparkles size={15} /> A calmer way to understand math</p><h1>Ask. Learn.<br /><em>Understand.</em></h1><p className="lede">A learning workspace for students, tutors, and curious minds. Choose how you want to begin, then continue in the same MathDesk AI chatbox.</p></div><section className="workspace-card" aria-labelledby="workspace-title"><div className="workspace-heading"><div><p className="eyebrow">Interactive workspace</p><h2 id="workspace-title">Start with a learning mode</h2></div><span className="online-dot">● online AI</span></div><div className="mode-grid" role="tablist" aria-label="Learning modes">{modes.map((item) => <button className={`mode-card ${mode === item.id ? 'active' : ''}`} key={item.id} onClick={() => setMode(item.id)} role="tab" aria-selected={mode === item.id}><strong>{item.label}</strong><span>{item.description}</span></button>)}</div><div className="prompt-row"><label className="sr-only" htmlFor="prompt">Math question</label><textarea id="prompt" value={prompt} onChange={(event) => setPrompt(event.target.value)} placeholder="Try: Explain why the derivative of x² is 2x" rows={2} /><button className="send-button" onClick={() => openWorkspace(mode, prompt)} aria-label="Open AI chatbox"><ArrowUpRight size={21} /></button></div><p className="status">Everything continues in the main AI chatbox, including uploads and saved history.</p><button className="workspace-link" onClick={() => openWorkspace(mode, prompt)}>Open full workspace <ArrowUpRight size={15} /></button></section></section><section className="feature-strip reveal-on-scroll" aria-label="MathDesk capabilities"><div className="marquee-strip" aria-hidden="true"><div className="marquee-track"><span>Upload a lesson</span><span>→</span><span>Ask Desky</span><span>→</span><span>Practice with purpose</span><span>→</span><span>Save your progress</span><span>→</span><span>Upload a lesson</span><span>→</span><span>Ask Desky</span><span>→</span><span>Practice with purpose</span><span>→</span><span>Save your progress</span></div></div><div className="feature-strip-grid"><article className="feature-card"><span className="feature-icon"><Upload size={18} /></span><h3>Bring the problem in</h3><p>Type, upload, photograph, or handwrite a question without leaving the workspace.</p></article><article className="feature-card"><span className="feature-icon"><BookOpen size={18} /></span><h3>See the reasoning</h3><p>Switch between solve, learn, and practice modes to match the way you need help.</p></article><article className="feature-card"><span className="feature-icon"><PenLine size={18} /></span><h3>Keep learning</h3><p>Save lessons locally and sync your progress when your account and connection are available.</p></article></div></section><ToolsDirectory /><footer className="footer"><span>Connected tools and chat foundation.</span><span>n8n + Supabase preserved.</span></footer></main>;
}
